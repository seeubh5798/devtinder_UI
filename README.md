# Dev Tinder Frontend
- using Vite + React project
- install Daisy UI
- Add Navbar in a separate component and add i t in App component
- setup routing in application using react-router-dom in npm
- BrowserRouter -> create Routes -> Create Route -> Create Route inside Route for children routes -> Use <Outlet> </Outlet> in parent <Body /> component to render children routes
- Create Login Page
- install cors in backend, add middleware with configurations and add withcredentials:true in frontend
-  push user data in redux store
- refreshing the page should not logout the user - in body component, check if we haven token or not-> if yes, go to
